---
import Layout from '../layouts/Layout.astro';
---

<Layout title='404 | Antoine Weverbergh | Front End Developer'>
    <main>
        <div class='container__error'>
            <div class='error'>
                <h1>4🤷‍♀️4</h1>
                <p>Page not found</p>
                <a href='/'>Go to home page</a>
            </div>
        </div>
    </main>
</Layout>

<style lang='scss'>
    @import '../styles/utils/_variables.scss';
    @import '../styles/utils/_mixins.scss';

    .container__error {
        height: 100vh;
        width: 100vw;
        position: relative;
        background-color: $black;
    }
    .error {
        text-align: center;
        color: $title-color;
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        h1 {
            @include rhythm(80, 100);
            margin-bottom: 30px;
            @include screen(tablet) {
                @include rhythm(120, 120);
            }
        }
        p,
        a {
            text-transform: uppercase;
            letter-spacing: 2px;
            font-family: $font-family-details;
            @include rhythm(18, 18);
        }
        a {
            display: inline-block;
            font-size: 16px !important;
            font-size: 18px;
            position: relative;
            top: 70px;
            padding: 12px 20px;
            border: 1px solid $title-color;
            transition: all 0.2s ease-in-out;
            &:hover {
                background-color: $title-color;
                color: $black;
            }
        }
    }
</style>
